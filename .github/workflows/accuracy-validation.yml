name: CineCalendar 3.6 Accuracy Validation

on:
  push:
    branches: ["feature/recommendation-accuracy-3.6.0"]

permissions:
  contents: read

jobs:
  validate:
    runs-on: windows-latest
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Validate 3.6 source tree
        shell: pwsh
        run: |
          $required = @(
            'cinecalendar\local_content_v36.py',
            'cinecalendar\recommender_v18.py',
            'cinecalendar\quality_manager_v36.py',
            'cinecalendar\accuracy_ui_v36.py',
            'tests\test_recommendation_accuracy_v360.py'
          )
          foreach ($p in $required) { if (!(Test-Path $p)) { throw "Missing 3.6 source: $p" } }
          $version = (python -c "from cinecalendar import __version__; print(__version__)").Trim()
          if ($version -ne '3.6.0') { throw "Unexpected version: $version" }

      - name: Install dependencies
        run: python -m pip install -r requirements-dev.txt

      - name: Compile sources
        run: python -m compileall -q cinecalendar scripts tests

      - name: Run tests
        env:
          PYTHONPATH: .
        run: python -m pytest -q

      - name: Benchmark 260k catalog engine
        env:
          PYTHONPATH: .
        run: python scripts/benchmark_engine_v3.py

      - name: Build Windows portable folder
        shell: pwsh
        run: |
          python -m PyInstaller --noconfirm --clean --onedir --windowed --name CineCalendar --manifest CineCalendar.manifest --hidden-import=sqlite3 --collect-all implicit launcher.py
          $exe = 'dist\CineCalendar\CineCalendar.exe'
          if (!(Test-Path $exe)) { throw 'EXE was not created' }
          $b = [IO.File]::ReadAllBytes($exe)
          if ($b.Length -lt 2 -or $b[0] -ne 0x4D -or $b[1] -ne 0x5A) { throw 'Not a Windows PE executable' }

      - name: Smoke launch
        shell: pwsh
        run: |
          $env:CINECALENDAR_DATA_DIR = Join-Path $env:RUNNER_TEMP 'cinecalendar-36-smoke'
          $p = Start-Process -FilePath '.\dist\CineCalendar\CineCalendar.exe' -PassThru
          Start-Sleep -Seconds 8
          if ($p.HasExited) {
            if ($p.ExitCode -ne 0) { throw "CineCalendar exited with $($p.ExitCode)" }
          } else {
            Stop-Process -Id $p.Id -Force
          }
